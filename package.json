{
  "name": "lumina-discord-bot",
  "version": "1.0.0",
  "description": "Bot de Discord completo con sistemas de verificación, bienvenidas personalizables, autoroles, embeds avanzados y soporte multi-base de datos (PostgreSQL, MongoDB, almacenamiento local)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js", 
    "dev": "cross-env NODE_ENV=development tsx watch src/index.ts",
    "dev:windows": "set NODE_ENV=development && tsx watch src/index.ts",
    "dev:unix": "NODE_ENV=development tsx watch src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push:pg",
    "db:studio": "drizzle-kit studio",
    "backup": "node -e \"const { SmartDatabaseManager } = require('./dist/database/smart-manager'); const db = new SmartDatabaseManager(); db.createBackup().then(console.log).catch(console.error);\"",
    "health": "node -e \"const { SmartDatabaseManager } = require('./dist/database/smart-manager'); const db = new SmartDatabaseManager(); db.init().then(() => db.healthCheck()).then(console.log).catch(console.error);\"",
    "postinstall": "npm run build",
    "clean": "cross-env rm -rf dist && rm -rf node_modules/.cache",
    "lint": "echo 'Linting not configured yet'",
    "test": "echo 'Tests not configured yet'"
  },
  "keywords": [
    "discord",
    "bot",
    "typescript",
    "spanish",
    "verification",
    "autoroles",
    "welcome",
    "goodbye",
    "embeds",
    "postgresql",
    "mongodb",
    "lumina"
  ],
  "author": "Lumina Bot Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/pg": "^8.10.0",
    "better-sqlite3": "^12.2.0",
    "cross-env": "^7.0.3",
    "discord.js": "^14.14.1",
    "dotenv": "^16.3.0",
    "drizzle-kit": "^0.20.0",
    "drizzle-orm": "^0.29.0",
    "mongoose": "^8.18.1",
    "pg": "^8.11.0",
    "sqlite3": "^5.1.7",
    "winston": "^3.11.0",
    "winston-daily-rotate-file": "^4.7.1",
    "zod": "^3.22.0"
  }
}
